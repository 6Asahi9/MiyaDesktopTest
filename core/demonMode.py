def toggle_demon_mode(enabled):
    if enabled:
        print("Demon Mode: ENABLED — Admin privileges and full access granted.")
        # Future logic: unlock advanced features, show confirmation, etc.
    else:
        print("Demon Mode: DISABLED — App access restricted to safe paths.")
